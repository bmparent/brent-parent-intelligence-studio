# Independent Works review — 2026-09-22

## Scope and finding
Reviewed frontend candidate #66 (40bccd1) and backend #63 (e76d8ee). These remain unmerged; unchanged production appearance is expected. Candidate implementation is substantial but not production-accepted.

## Repairs
- Reject missing frozen kit archives rather than silently delivering historical package bytes; preserve genuine legacy orders.
- Preserve a completed Snapshot report when image-stage worker execution is interrupted; do not replay uncertain provider calls.
- Restore Playground, Lab and Community header links.
- Remove global service-page CSS overrides that defeated adaptive liquid glass. Keep navigation above optical layers and preserve mobile placement.
- Replace internal acceptance and implementation jargon in customer-facing service copy.
- Explain unavailable Snapshot images without implying they are still processing.
- Synchronize generated Playground glass styles.

Frontend follow-up: https://github.com/bmparent/brent-parent-intelligence-studio/pull/67
Backend vendor mirror: https://github.com/bmparent/eidos/pull/64

## Evidence and limits
Before the workspace interruption, independent local runs passed audit 30, platform 29, backend 22, focused delivery 8, and glass 6 tests, plus lint. Suites overlap; totals are not a coverage percentage. Browser checks exercised the business concepts, filtering, estimate changes and mobile menu Escape behavior at a 390px frame without horizontal overflow. These checks do not prove physical-device or provider acceptance.

Workspace maintenance removed the uncommitted checkouts. Fixes and regression tests were reconstructed on the follow-up branches. Fresh GitHub CI, rather than the earlier local results, must validate the reconstructed revisions. The first CI run caught an unsynchronized generated glass export; that has been corrected. Consult the PR checks for current outcome.

## Remaining production gates
- Paired hosted frontend/backend, isolated preview database and migrations.
- Real Google consent, account linking and inbox delivery.
- Stripe TEST checkout, webhook replay, refund/dispute and authenticated package recovery. Connected Stripe accounts exposed live mode only during review; no charges made.
- Hosted Playground save/reopen/export and AI proposal acceptance.
- Snapshot capture DNS-rebinding boundary remains unresolved; keep generation disabled.
- Original private PERNR/Promo capture evidence, physical-device/accessibility acceptance.
- Affiliate identity/products and commercial allowances remain unapproved; do not activate on assumptions.

Production was not changed by this review.
