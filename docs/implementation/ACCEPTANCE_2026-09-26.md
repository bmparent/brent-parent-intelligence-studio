# Eidos Works paired preview acceptance — 2026-09-26

**Public release decision: NO-GO.** The owner command center is an isolated, Access-protected preview. This record updates the exact paired candidate and distinguishes current provider/HTTP checks from earlier authenticated browser acceptance. It does not approve a PR merge, production domain switch, paid AI, Snapshot generation, customer message, or LIVE Stripe action.

## Exact candidate

| Component | Candidate | September 26 evidence |
| --- | --- | --- |
| Public Pages site | `https://eidosworks-test-20260923.pages.dev`; deployment `00024a26-e9bb-46db-9ec7-4bbb3ff11c16`; source `3688ad81e4fc500887931860dec22db780b1a51f` | Stable root and new unique deployment URL both returned 200 with the same HTML SHA-256, `8753d5468436fba7eeed3f0be34e13aad2b5625ea7311f1eeacf1d11fa397840`. `/api/public-config` reported accounts, Google, AI, community, passwords, and shop configured with `localTest=false`. Configuration is not transaction acceptance. Direct Upload's generated build tree was dirty; the source commit is the PR head, not a claim of a clean build tree. |
| Owner backend | Vercel deployment `dpl_4B4HQaAbfyRAuiC6UPAXBjxRJHex`; source `1bbcab6bf1e2483d8a769bf12534039c397a994d`; `https://eidos-sentinel-qicep692a-1brentbm-1876s-projects.vercel.app` | Vercel reported Git source and READY. Backend PR #65 quality runner and verify checks passed on this head. The preview prefers Vercel's system commit revision; the authenticated owner overview reported this exact source commit. |
| Private owner Worker | `https://owner-preview.eidos-works.com`; version `0301ce45-8bc2-45e2-b273-5a287215db70` | Wrangler deployments listed this version at 100% traffic. Its checked-in configuration points to the backend above and the isolated Pages hostname. Anonymous HTML and API requests redirected to exact-host Cloudflare Access; the disabled `workers.dev` address returned 404. After a fresh TOTP challenge, the owner opened the console and its overview reported backend source `1bbcab6bf1e2483d8a769bf12534039c397a994d`, healthy validation database, four members, eight open work items, and TEST store mode. |

The Pages project has no Eidos production custom domain. The public `eidos-works.com` alias, existing DG Promo Photos application/policies, and shared Zero Trust organization were not changed. Cloudflare's login domain and global login branding are shared by that account, while the app heading and exact protected hostname identify Eidos Works.

## Tests and connector state

- On site source `3688ad8`, `npm run build`, `npm run verify:prerender`, and `npm run test:audit` passed; the audit suite had 35/35 passing tests. `npm run test:owner-console` passed 5/5 and both site and Snapshot Worker type checks passed. The clean-source hosted Chromium run passed 26/26 route and interaction checks at 390, 768, and 1440 CSS pixels with reduced motion. The same [26/26 browser checks on the current Pages deployment](../../artifacts/implementation/2026-09-26/hosted-public-after-mailer/hosted-public-browser.json) passed after mailer binding, with six new screenshots retained. Viewport emulation is not a physical phone, actual 200% browser zoom, or a screen reader.
- The backend source at `1bbcab6` passed `npm --prefix apps/sentinel-lab test` from the repository root: 16/16 JavaScript and 60/60 TypeScript tests, including the capture and owner operations cases. Lint and exact-head GitHub quality checks also passed. A fresh [paired vendored contract report](../../artifacts/implementation/2026-09-26/owner-console-parity.json) matched 76/76 files between the site build worktree at base `3688ad8` and backend head `1bbcab6`. The report records working-tree hashes because site build generators rewrite some tracked files; it is not a clean Git-tree or hosted transaction proof.
- A local live proxy smoke fetched `https://example.com/` successfully after fixing Node 24's DNS lookup callback shape. The protected Snapshot proxy has source-level public-IP pinning, per-redirect revalidation, response limits, and a dedicated token requirement. The matching token is **not** configured on the Vercel/Page pair, so hosted capture egress is unaccepted. `SNAPSHOT_CAPTURE_APPROVED` and `SNAPSHOT_GENERATION_ENABLED` remain false.
- The newest authenticated console read found both controlled `@example.invalid` accounts; one detail showed zero projects, paid Playground entitlements, and active sessions. The work queue saved and reopened a note correcting the stale Google callback description while leaving personal consent blocked. Stripe TEST read-only reconciliation observed four kit ledger rows and zero mismatches within the bounded provider window; webhook delivery remains unavailable. GitHub PR/head/workflow data were healthy; Cloudflare Pages and Vercel in-console status are exact-URL HTTP probes only. Provider deployment metadata and logs, authoritative inbox/CRM reads, Drive/Sheets, GA4/Search Console, and AI provider usage remain disconnected **inside the hosted console** without restricted server-side read grants. The separate Gmail connector confirmed one labeled QA message from this exact Pages deployment, as recorded below; that does not make the hosted console an inbox reader.
- The current Worker version corrected the console's release link to this September 26 record. After versioned deployment at 100%, an authenticated reload showed the new blocked-gate date and exact record URL, and the overview still reported backend source `1bbcab6`. In the narrow owner view, keyboard Enter opened Menu with focus on Overview; Escape closed it and returned focus to Menu. This is not actual 200% browser zoom or a native screen reader. The release link correction passed `npm run test:owner-console` (5/5) and `npm run typecheck` before upload.
- The exact preview Google OAuth redirect is registered and a fresh provider start previously returned 200 without a redirect mismatch. Personal consent and an Eidos session are still pending. The Stripe MCP connector currently asks for reauthentication, but the existing browser Dashboard session reported `livemode=false`, identified the Test mode account, and displayed an event-delivery destination at this isolated site's `/api/shop/webhook`. Historical deliveries do not prove a new checkout, archive entitlement, replay, refund, or dispute on this deployment.
- The isolated Pages project now has one production `EIDOS_INQUIRY_MAILER` service binding to the existing private `eidos-works-inquiry-mailer` Worker, version `2ab25d22-171a-4641-821d-ebc42aee7168`. The scoped Cloudflare API patch preserved all three existing Pages environment variables and other project settings. Wrangler Direct Upload redeployed the same built HTML and source commit as deployment `00024a26-e9bb-46db-9ec7-4bbb3ff11c16`; Cloudflare confirmed it is the latest deployment. Exact `/api/project-inquiries` readiness returned 200 with `deliveryConfigured=true` and `privateMailerConfigured=true`. One `Eidos Works QA - not a customer lead` inquiry returned `state=sent`, receipt `0e3889e4-dd3c-4a7c-85c9-4d50df63b003`, and `measurementRecorded=false`. Gmail independently found message `1a0df68e126aa6dd` in the studio inbox and its body contained the same receipt and QA marker. This proves one delivery to the controlled inbox; it does not prove general inbox/CRM connector reads or measurement recording. Wrangler's incomplete experimental downloaded config was not reapplied. The DG Promo Photos app and production `eidosworks` Pages project were not altered.
- The Vercel connector found no grouped runtime errors in the project over its selected last-day window. On deployment `dpl_4B4HQaAbfyRAuiC6UPAXBjxRJHex`, its preview status-code grouping returned ten 200s, one 201, and one 405 in the observed window. This is a provider read outside the hosted console; the console's own Vercel card remains an HTTP availability probe.

## Remaining release gates

1. Finish exact-pair owner console acceptance: the newest overview, account/work reads, note write/reopen, TEST reconciliation, and service probes passed. Recheck keyboard, actual 200% zoom, and any required mutation negative cases on this exact revision; the earlier guarded mutation matrix remains historical. Do not use a bypass or a real customer record.
2. Complete personal Google sign-in/consent and account terms on the isolated site. Then verify account recovery, email receipt, identity linking, cross-account isolation, and authenticated Playground save/reopen, media ownership, proposal/apply/Undo, and entitled export. The isolated Pages private mailer and one controlled inbox receipt passed; authoritative inbox/CRM reads within the owner console remain disconnected.
3. Complete a new Stripe **TEST** checkout on this Pages deployment, then prove signed webhook delivery, kit archive digest/download, guest recovery, replay handling, and TEST refund/dispute revocation. Any checkout terms acceptance and final transaction action require the owner. Keep LIVE entirely separate.
4. Test an actual phone, actual 200% browser zoom, and a screen reader. Keep paid AI/authoring disabled until bounded hosted acceptance and cost allowance exist.
5. Configure matching restricted Snapshot proxy tokens and prove hosted pinned egress, then complete the cost ceiling, storage bindings/migration, existing order recovery, retention/support policy, and signed TEST fulfillment. Leave generation and orders off until those independent gates pass.

The machine-readable [paired preview receipt](../../artifacts/implementation/2026-09-26/paired-preview-receipt.json) records the observed deployments and explicit unknowns. Both PRs remain draft: [site #68](https://github.com/bmparent/brent-parent-intelligence-studio/pull/68) stacked on #66 and [backend #65](https://github.com/bmparent/eidos/pull/65) stacked on #63.
