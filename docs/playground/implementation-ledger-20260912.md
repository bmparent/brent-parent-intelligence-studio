# Works handoff — September 12, 2026

The editor releases are complete and live. The entire brief is **partial**: account upgrades are reviewable and tested, but hosted identity, successful contact delivery and real Stripe TEST acceptance remain blocked on external prerequisites.

## Shipped

- A: [PR43](https://github.com/bmparent/brent-parent-intelligence-studio/pull/43) repaired direct gestures, stable preview/glass identity, cancellation and mobile canvas behavior.
- B: [PR44](https://github.com/bmparent/brent-parent-intelligence-studio/pull/44) added explicit v4 responsive hero/content blocks, independent images/text/buttons, stack/row/column moves, frame resize versus photo crop, inline text, duplication, layer controls, independent mobile adjustments and portable exports.
- Live: https://eidos-works.com/playground/ — merged source `6a47244d68c0ff75ecb5665d9155b17cad95d67a`; Pages deployment `ae4ff385-7638-4c5a-a619-72562bf95372`.
- Accepted CI merge `75117222e63b7849a0fb3db21fd59ce575bdcf4e` and final main have identical tree `6065fd7af15f99a756bbce8b9425290576ef79c9`. Canonical entry JS/CSS and Playground chunk bytes match the accepted artifact. Provider configuration fingerprints match before/after B deployment.

Older project formats remain unchanged on opening, and an explicit upgrade is reversible. Advanced formats remain local-only until hosted storage acceptance. No frozen purchase archive, unrelated checkout, approved glass or research behavior was changed.

## Validation and meaning

Six engine/width cases passed for both the existing-format and v4 editor in CI. Production Chromium desktop and real CDP touch passed actual move/resize/crop, cancellation, Undo/Redo, mobile override/reset, local reload, malformed import preservation and actual ZIP download. All gestures retained root/glass nodes and made zero transient persisted writes. The operation model commits one validated change; Undo restores the exact document.

Additional evidence: 30 large-project/responsive/text-scale cases (96 nodes, nine widths, three engines); 12 independently hosted ZIP cases with JavaScript enabled/disabled; 63 public journey cases and 21 repeated after release. Contact required-field validation, cross-origin denial, honeypot rejection and direct-email fallback passed. Actual Google Analytics page_view traffic was observed after consent, with private marker/account-route exclusions; reporting-console ingestion and successful generate_lead delivery are not claimed.

Timing is input-to-RAF-style-update, not physical display latency. CI p95 ranged 0.4–23ms with frame p95 16.7–18ms on the assumed 60Hz baseline. Production desktop p95 was 0.9ms; isolated touch was 1.4ms, frame p95 16.8ms, no recorded long tasks. A loaded-host touch run recorded 51, 67, 95 and 200ms long tasks; attribution remains unresolved, and that evidence is retained. Empty long-task arrays in engines lacking Long Tasks API support do not prove their absence. Physical iPhone and browser-toolbar zoom remain untested; CSS text at 200% was tested.

## Account work preserved for completion

[Frontend PR46](https://github.com/bmparent/brent-parent-intelligence-studio/pull/46) and [backend PR61](https://github.com/bmparent/eidos/pull/61) reconcile the existing held branches. They preserve the identity model, real local password/Google/image adapters, strict schemas, owner checks, immutable revisions and expected-head conflicts. An account-change nonce invalidates late responses across tabs without replacing the local design. Server authorization remains authoritative.

52 frontend tests and 64 backend tests passed. Actual local signup/login/dashboard flows passed in two contexts; delayed A-open/A-save responses across real B signup/login preserved the exact local document and denied cross-owner access. All 66 exported portable files matched source at validation. Frontend CI, backend CI and the existing Git-triggered Vercel preview build passed. Local default Turbopack could not follow the dependency junction; webpack passed, and the remote preview built with Turbopack. These are separate pieces of evidence.

The backend preview is `dpl_7a7gwFdLzvPabFrZ6vp8WwcE6o6n`, source `ecd6b51504c4d84f4828b047a3129cd554b67057`, target preview. It is not a production account release. Current canonical legacy accounts/assistant/shop remain available; the older isolated account preview reports new account providers unavailable. No claim that all account services are globally disabled is made.

## Remaining gates and smallest owner actions

1. Provide owner-controlled test inboxes and complete real confirmation/reset/contact delivery evidence. UI success alone will not count as delivery. No prospect outreach or QA email was sent.
2. Use an existing authorized Google client/callback for legitimate hosted consent and safe linking. No new Google Cloud resource is permitted by this brief.
3. Make existing Stripe TEST configuration available through approved secret handling; run actual test checkout, webhook retries, refunds/revocation and immutable archive checks. No plaintext secrets in chat, live charge, price change or live Playground sale.
4. After those prerequisites: hosted fresh-context image hydration/reopen, revisions/restore/save-as-new, concurrent expected-head conflicts, expiration/offline/reconnection and new-format owner isolation must pass before account rollout. No remote data migration was performed.

## Reproducible release and artifacts

Main workflow `34713407778` passed its application gates and stopped at the explicitly missing Cloudflare Actions credentials. Existing authenticated Wrangler CLI deployed the accepted build instead. The repeatable path is: pass applicable CI, compare the tested tree to current main, extract that exact source and accepted dist, read provider configuration, deploy through existing OAuth, then verify source/assets/config and real canonical browser flows. Preflight and deployment are separate operations; a failed preflight must stop release. No token or subscription was created.

Configured repository commands run: typecheck, lint, test:playground, test:platform, test:glass, test:analytics, test:snapshot, build, build:functions, verify:prerender, verify:editorial, verify:urls and validate:insights:dist. Dedicated composition/workspace/authoring/browser tests supplement aggregate commands. Backend commands and the local fixture rerun steps are in the account reconciliation document and proof package.

Raw evidence is retained under `C:/Users/bmpar/works-evidence-20260912/`, including failures. `completion-receipt.json` links counts, exact deployment identity and raw timing receipts. Backend repo-local artifacts, journal and Proof Logic + Meaning are under `C:/Users/bmpar/codex-worktrees/works-accounts-backend-20260912/artifacts/works_accounts_20260912/` and `docs/proof_runs/2026-09-12/works-accounts/`. Exact receipt bytes were verified against committed Git blobs.

The account proof package was copied and hash-checked at `G:/My Drive/Eidos_Brain_Proof_Phase/2026-09-12/works-accounts-reconciliation/`. The release handoff and selected receipts are mirrored separately under `G:/My Drive/Eidos_Works_Releases/2026-09-12/works-editor/`. These are filesystem copy receipts; independent Google Drive sync completion was not inspected.

Proof principle: reproducibility makes a result revisitable, while owner equality and compare-and-swap keep records trustworthy. This improves the Works product and its evidence trail. It does not prove reservoir, compression, anomaly-detection or other Eidos Brain research claims. Task-created local preview/fixture processes were stopped after their checks; source, traces, archives and original branches remain available.
